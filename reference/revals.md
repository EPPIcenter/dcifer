# Parameter grid

Precalculated parameter grids for a range of values of M (from 1 to 5).

## Usage

``` r
revals
```

## Format

A list of length 5, where each element corresponds to a single value of
M and is a matrix with M rows. Each column of a matrix is a r₁ = ... =
r_(M)) combination.
