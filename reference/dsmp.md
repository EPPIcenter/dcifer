# Sample data

Microhaplotype data from two clinics in Mozambique. Samples are sorted
by location.

## Usage

``` r
dsmp
```

## Format

A list of 52 elements (samples), each element is a list of 87 elements
(loci), which are integer vectors (alleles).
